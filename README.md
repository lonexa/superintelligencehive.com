# superintelligencehive.com

Landing page for **SI Hive (Superintelligence Hive)**, served by GitHub Pages.

Plain static files: `index.html`, `styles.css`, `main.js` and screenshots in `assets/`. No build step. `CNAME` points Pages at the custom domain.

Preview locally by opening `index.html` in a browser.
